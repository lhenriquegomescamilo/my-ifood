version: "3.7"
services:
  postgres_register:
    container_name: postgres_register
    image: postgres:12.4
    environment:
      POSTGRES_USER: register
      POSTGRES_PASSWORD: register
    ports:
      - "5432:5432"